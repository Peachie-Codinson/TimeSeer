{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main window. Remote content only ever gets notification/opener access, scoped to the exact deployed origin — never shell, filesystem, or generic HTTP access (spec 5.5).",
  "windows": ["main"],
  "remote": {
    "urls": ["https://planner.example.com/*"]
  },
  "permissions": ["core:default", "notification:default", "opener:default"]
}
